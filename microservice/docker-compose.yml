services:
  postgres-account:
    image: postgres:17-alpine
    container_name: fruitshop-account-db
    command: ["postgres", "-p", "5436"]
    environment:
      POSTGRES_DB: account_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5436:5436"
    volumes:
      - account_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -p 5436 -U postgres -d account_service"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fruitshop-micro-network

  postgres-payment:
    image: postgres:17-alpine
    container_name: fruitshop-payment-db
    command: ["postgres", "-p", "5437"]
    environment:
      POSTGRES_DB: payment_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5437:5437"
    volumes:
      - payment_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -p 5437 -U postgres -d payment_service"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fruitshop-micro-network

  postgres-review:
    image: postgres:17-alpine
    container_name: fruitshop-review-db
    command: ["postgres", "-p", "5438"]
    environment:
      POSTGRES_DB: review_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5438:5438"
    volumes:
      - review_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -p 5438 -U postgres -d review_service"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fruitshop-micro-network

  postgres-ai:
    image: postgres:17-alpine
    container_name: fruitshop-ai-db
    command: ["postgres", "-p", "5439"]
    environment:
      POSTGRES_DB: ai_service
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5439:5439"
    volumes:
      - ai_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -p 5439 -U postgres -d ai_service"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fruitshop-micro-network

  discovery-server:
    build:
      context: ./discovery-server
      dockerfile: Dockerfile
    container_name: fruitshop-discovery-server
    ports:
      - "8761:8761"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      EUREKA_CLIENT_REGISTER_WITH_EUREKA: "false"
      EUREKA_CLIENT_FETCH_REGISTRY: "false"
      EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE: http://discovery-server:8761/eureka/
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8761/"]
      interval: 15s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fruitshop-micro-network

  flyway-account:
    image: flyway/flyway:11-alpine
    container_name: fruitshop-flyway-account
    command:
      - -url=jdbc:postgresql://postgres-account:5436/account_service
      - -user=postgres
      - -password=postgres
      - -connectRetries=30
      - -locations=filesystem:/flyway/sql
      - migrate
    volumes:
      - ./account-service/src/main/resources/db/migration:/flyway/sql:ro
    depends_on:
      postgres-account:
        condition: service_healthy
    networks:
      - fruitshop-micro-network

  account-service:
    build:
      context: ./account-service
      dockerfile: Dockerfile
    container_name: fruitshop-account-service
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-account:5436/account_service
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      EUREKA_DEFAULT_ZONE: http://discovery-server:8761/eureka/
    depends_on:
      postgres-account:
        condition: service_healthy
      discovery-server:
        condition: service_healthy
      flyway-account:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - fruitshop-micro-network

  apigateway-server:
    build:
      context: ./apigateway-server
      dockerfile: Dockerfile
    container_name: fruitshop-apigateway-server
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE: http://discovery-server:8761/eureka/
    depends_on:
      discovery-server:
        condition: service_healthy
      account-service:
        condition: service_started
    restart: unless-stopped
    networks:
      - fruitshop-micro-network

volumes:
  account_postgres_data:
  payment_postgres_data:
  review_postgres_data:
  ai_postgres_data:

networks:
  fruitshop-micro-network:
    driver: bridge