spring.application.name=FruitShop

# ============================
# DATABASE (PostgreSQL Railway)
# ============================

# JDBC URL từ biến môi trường Railway
spring.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}

# Username & Password từ Railway
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}

# PostgreSQL Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# Railway internal connection - no SSL needed
spring.datasource.hikari.data-source-properties.ssl=false

# ============================
# HIBERNATE / JPA
# ============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# HikariCP Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.connection-init-sql=SET TIME ZONE 'UTC'


# ============================
# SERVER CONFIGURATION
# ============================
# Railway sẽ truyền PORT qua env → bắt buộc dùng PORT env
server.port=${PORT:8080}

# Encoding
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true


# ============================
# JSON & LOCALE
# ============================
spring.jackson.time-zone=Asia/Ho_Chi_Minh
spring.jackson.locale=vi_VN
spring.jackson.date-format=dd/MM/yyyy HH:mm:ss
spring.jackson.default-property-inclusion=non_null
spring.jackson.serialization.write-dates-as-timestamps=false


# ============================
# LOGGING
# ============================
logging.level.org.springframework.data.jpa=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE 


# ============================
# FILE UPLOAD
# ============================
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB


# ============================
# CLOUDINARY
# ============================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}
cloudinary.secure=true


# ============================
# Momo Payment Sandbox Config
# ============================

momo.partner-code=${MOMO_PARTNER_CODE:MOMO}
momo.access-key=${MOMO_ACCESS_KEY:F8BBA842ECF85}
momo.secret-key=${MOMO_SECRET_KEY:K951B6PE1waDMi640xX08PD3vg6EkVlz}

# Endpoint API
momo.endpoint=${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api}

# Loại request thanh toán
momo.request-type=payWithATM

# Redirect về frontend sau khi thanh toán
momo.return-url=${MOMO_RETURN_URL:https://fruitshop-c4.vercel.app/customer/orders}

# IPN callback từ MoMo gửi về backend
momo.ipn-url=${MOMO_IPN_URL:https://fruitshop.up.railway.app/api/momo/ipn-handler}


# ============================
# SPRING BOOT ACTUATOR
# ============================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.db.enabled=true
